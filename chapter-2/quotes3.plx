#!/usr/bin/perl
#quotes3.plx
use warnings;
print"It's as easy as that.\n";
#We don't need to use backslashes to escape single quotes in a double quoted string.
print '"Stop," he cried.',"\n"
#Since we can pass multiple strings to print with the , character, we can use a combination of single and 
#double quoted strings depending on whether or not we want escape characters to be processed or variables
#to be interpolated for each section of text.

